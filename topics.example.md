# Topics for /ae-research
#
# One topic per line. Rules:
#   - Blank lines are ignored.
#   - Lines starting with `#` are comments (like these) and are skipped.
#   - Leading list markers (`- `, `* `, `1. `) are stripped, so a Markdown
#     bullet list works as-is.
#
# Copy this file to `topics.md` (or pass a path) and edit, then run:
#   /ae-research             # batch over topics.md
#   /ae-research @my-list.md # batch over a custom file (note the leading @)

- What are the current best practices for securing a Kubernetes cluster in 2026?
- How does eBPF compare to traditional iptables for service-mesh data planes?
- What is the state of the art in passwordless authentication for enterprise SSO?
